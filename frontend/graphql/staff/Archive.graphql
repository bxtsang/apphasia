mutation ($id: Int!, $archive_reason: String, $is_active: Boolean!) {
  update_staffs_by_pk (
    pk_columns: {id: $id}
    _set: {
      is_active: $is_active,
      archive_reason: $archive_reason
    }
  ) {
    id
  }
}
