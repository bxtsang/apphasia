query getAllEvents($project_id: Int!) {
  events(where: {project_id: {_eq: $project_id}}) {
    date
    start_time
    end_time
    id
    name
    note
    project_id
    project {
      title
    }
  }
}

# {
#   "project_id": 2
# }