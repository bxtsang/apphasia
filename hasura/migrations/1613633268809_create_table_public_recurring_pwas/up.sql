CREATE TABLE "public"."recurring_pwas"("recurring_id" integer NOT NULL, "pwa_id" integer NOT NULL, "project_id" integer NOT NULL, PRIMARY KEY ("recurring_id","pwa_id","project_id") , FOREIGN KEY ("recurring_id", "project_id") REFERENCES "public"."recurring"("id", "project_id") ON UPDATE restrict ON DELETE cascade, FOREIGN KEY ("pwa_id", "project_id") REFERENCES "public"."project_pwa"("pwa_id", "project_id") ON UPDATE restrict ON DELETE cascade);
