CREATE TABLE "public"."pwa"("id" serial NOT NULL, "email" text NOT NULL, "name" text NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), "dob" date NOT NULL, "gender" bpchar NOT NULL, "contact_num" integer NOT NULL, "address" text, "last_stroke" text NOT NULL, "wheelchair" text NOT NULL, "hobbies" text NOT NULL, "channel" text NOT NULL, "consent" boolean NOT NULL, "date_joined" date NOT NULL, "notes" text, PRIMARY KEY ("id") , UNIQUE ("email"));
