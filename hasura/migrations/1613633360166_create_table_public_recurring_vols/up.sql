CREATE TABLE "public"."recurring_vols"("recurring_id" integer NOT NULL, "project_id" integer NOT NULL, "vol_id" integer NOT NULL, PRIMARY KEY ("recurring_id","project_id","vol_id") , FOREIGN KEY ("recurring_id", "project_id") REFERENCES "public"."recurring"("id", "project_id") ON UPDATE restrict ON DELETE cascade, FOREIGN KEY ("project_id", "vol_id") REFERENCES "public"."project_vol"("project_id", "vol_id") ON UPDATE restrict ON DELETE cascade);
